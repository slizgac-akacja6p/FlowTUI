# M2: AI Integration — Tasks

## Scope
invoke_and_measure(), streaming, planning pipeline v1, chat, sprint context, graceful degradation, dry-run.

## Tasks

### T2.1: SPIKE — CC -p file creation
BLOCKING: Zweryfikuj czy `claude --dangerously-skip-permissions -p "prompt"` tworzy pliki w repo czy tylko odpowiada na stdout.
Test: uruchom CC z promptem "create file test.txt with content 'hello'" w tmp dir. Sprawdź czy plik powstał.
Wynik: udokumentuj w docs/plans/cc-p-spike.md. Jeśli CC nie tworzy plików → FlowTUI musi parsować stdout i sam tworzyć TASK-XXX.md.
Status: DONE — CC creates files when CLAUDECODE env var is stripped. SubprocessInvoker strips CLAUDECODE.

### T2.2: SubprocessInvoker
`flowtui/core/invoker.py` — CLIInvoker Protocol + SubprocessInvoker: invoke() i invoke_streaming() przez asyncio.create_subprocess_exec. Timeout handling (kill na przekroczeniu). Heartbeat check (60s silence = kill + error).
Status: DONE

### T2.3: PromptBuilder
`flowtui/core/prompt_builder.py` — build_prompt(tool, task, action, config, project_root): ładuje template z planning/prompts.py, wstrzykuje kontekst (project name, stack, architecture.md, current tasks).
`flowtui/planning/prompts.py` — szablony promptów: PLAN_DRAFT, CODE_TASK, REVIEW_TASK.
Status: DONE

### T2.4: Complexity routing
`flowtui/core/complexity.py` — estimate_complexity(description: str) → "trivial" | "simple" | "complex". Keyword-based heurystyka z PRD. Wpływa na długość promptu (trivial=krótki, complex=pełny kontekst).
Status: DONE

### T2.5: Planning pipeline v1
`flowtui/planning/pipeline.py` — plan_feature(description, config, invoker, task_mgr, project_root): 1 wywołanie CC, parsowanie stdout (jeśli CC -p nie tworzy plików: parse + create TASK-XXX.md), zwraca list[Task] w statusie DRAFT.
`flowtui/planning/parser.py` — parse_draft_tasks(stdout: str) → list[TaskDraft] (fallback gdy CC nie tworzy plików).
Status: DONE

### T2.6: Graceful degradation
`flowtui/analytics/limits.py` — is_tool_available(tool) sprawdza today_usage < budget. Fallback chain v1: jeśli CC niedostępny → skip z warningiem. DegradedMode dataclass z listą skipped operations.
Status: DONE

### T2.7: Komendy plan/code/review
W Orchestrator (core/engine.py): plan(description), code(task_id), review(task_id).
W TUI dispatch_command(): obsługa "plan", "code", "review", "plan --approve", "plan --reject".
Streaming output do TerminalPanel podczas wywołań CC.
Status: DONE

### T2.8: chat [model] z app.suspend()
Komenda `chat` i `chat claude` w TUI: przed suspend aktualizuje docs/context/sprint.md, wywołuje app.suspend() + subprocess CC z CWD=project_root, po powrocie wznawia TUI.
Status: DONE

### T2.9: docs/context/sprint.md writer
`flowtui/core/context_writer.py` — write_sprint_context(project_root, tasks, limits): zapisuje mały plik (~20-30 linii) z aktualnym stanem sprintu. Wywoływany przed każdym `chat` i po każdym zakończonym tasku.
Status: DONE

### T2.10: Dry-run mode
Flaga --dry-run w CLI i komendach: zamiast wywołania CC wyświetla tabelę "co by zostało zrobione" (prompt preview, context files, CWD, estimated tokens).
Status: DONE

### T2.11: Tests M2
Testy dla: SubprocessInvoker (z MockCLI), PromptBuilder (template rendering), complexity routing (keyword detection), planning pipeline (mock CC output → tasks), graceful degradation (budget exceeded → skip), context writer.
Status: DONE
