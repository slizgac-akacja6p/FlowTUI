# M5: Polish — Tasks

## Tasks

### T5.1: CSV/JSON export
`stats --export csv` i `stats --export json` — eksportuje analytics do pliku w .flowtui/exports/. Używa analytics/stats.py danych.
Status: TODO

### T5.2: Error handling edge cases
Heartbeat check na subprocess (60s silence → kill + error). Graceful Ctrl+C: kill subprocess + rollback + przywróć status taska. `flowtui exec` blokuje input poza `Ctrl+C` i `status` podczas `run sprint`.
Status: TODO

### T5.3: MockCLI
`tests/mock_cli.py` — MockCLI: implementacja CLIInvoker Protocol zwracająca predefiniowane outputy. Fixtures w conftest.py: mock_cli, tmp_project (tmp git repo z .flowtui/config.toml).
Status: TODO

### T5.4: headless mode (flowtui exec)
`flowtui exec "komenda"` — uruchamia komendę bez TUI (stdout/stderr standardowe). Przydatne do automatyzacji i testowania w CI.
Status: TODO

### T5.5: Tests M5 + regresja
Pełny test suite z MockCLI. Testy regresyjne end-to-end: plan → approve → run → merge. Coverage >80%.
Status: TODO
