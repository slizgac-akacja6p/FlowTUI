# Scenariusze testowe — M2: AI Integration

| # | Zadanie | Scenariusz (krok po kroku) |
|---|---------|---------------------------|
| 2.1 | Plan z streaming | 1. Wpisz `plan "dodaj walidację email"` 2. Streaming CC w TerminalPanel 3. Po zakończeniu: tabela DRAFT tasków z tytułami + prompt `plan --approve / plan --reject` |
| 2.2 | Approve planu | 1. Po 2.1 wpisz `plan --approve` 2. Taski DRAFT → TODO 3. TASKS panel odświeża się automatycznie |
| 2.3 | Dry-run | 1. Wpisz `plan "opis" --dry-run` 2. Tabela: co by zostało zrobione (prompt, context files, CWD) bez wywołania CC |
| 2.4 | Degraded mode | 1. Ustaw `claude_daily_budget = 0` w config.toml 2. Wpisz `plan "X"` 3. Warning: `⚠ DEGRADED MODE: CC limit reached → Planning: SKIPPED` |
